disallowed-methods = [
    { path = "std::option::Option::unwrap", reason = "use ? or .log_err() per CLAUDE.md" },
    { path = "std::result::Result::unwrap", reason = "use ? or .log_err() per CLAUDE.md" },
]
